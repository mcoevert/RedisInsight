export enum NotificationType {
  Global = 'global',
}

export enum NotificationServerEvents {
  Notification = 'notification',
  Feature = 'feature',
}

export enum NotificationEvents {
  NewNotifications = 'new-notifications',
  UpdateFeatureList = 'update-feature-list',
}
